@model DateTime?

@if(Model.HasValue)
{
    @Html.TextBox("", Model.Value.ToString("yyyy-MM-dd"), new { @class = "form-control"} )
}
else
{
    @Html.TextBox("", null, new { @class = "form-control" })
}
